query wordOfTheDay {
    wordOfTheDay {
        word_en,
        word_pl,
        type,
        definition_en,
        examples
    }
}

mutation reportWord($wordId: Float!, $reason: String!) {
    saveWordReport(wordId: $wordId, reason: $reason)
}